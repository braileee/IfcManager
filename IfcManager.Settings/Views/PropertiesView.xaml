<UserControl x:Class="IfcManager.Settings.Views.PropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IfcManager.Settings.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>

        <ScrollViewer ScrollViewer.CanContentScroll="True">
            <StackPanel >

                <!-- Add Property Set -->
                <Button Content="Add Property Set"
                    Command="{Binding AddPropertySetCommand}"
                    Margin="5"/>

                <ItemsControl ItemsSource="{Binding PropertySets}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>

                            <Border BorderBrush="Gray" BorderThickness="1" Margin="5">
                                <StackPanel>

                                    <!-- Property Set Header -->
                                    <DockPanel Background="#EEE">
                                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                             FontWeight="Bold"
                                             Width="200"
                                             Margin="5"/>

                                        <Button Content="Add Property"
                                            Command="{Binding DataContext.AddPropertyCommand,
                                                      RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Margin="5"/>

                                        <Button Content="Remove Set"
                                            Command="{Binding DataContext.RemovePropertySetCommand,
                                                      RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Margin="5"/>
                                    </DockPanel>

                                    <!-- Properties Grid -->
                                    <ItemsControl ItemsSource="{Binding Properties}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="5">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="150" />
                                                        <ColumnDefinition Width="80" />
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Property Name -->
                                                    <TextBox Text="{Binding PropertyName, UpdateSourceTrigger=PropertyChanged}"/>

                                                    <!-- Data Type -->
                                                    <ComboBox Grid.Column="1"
                                                          ItemsSource="{Binding DataContext.DataTypes, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                          SelectedItem="{Binding DataType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                                                    <!-- Remove -->
                                                    <Button Grid.Column="2"
                                                        Content="X"
                                                        Command="{Binding DataContext.RemovePropertyCommand,
                                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}" />
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                </StackPanel>
                            </Border>

                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </StackPanel>
        </ScrollViewer>

        <StackPanel Orientation="Horizontal" Margin="2">
            <Button Content="Save" Command="{Binding SaveCommand}" Padding="2" Margin="2"/>
        </StackPanel>
    </StackPanel>
</UserControl>
