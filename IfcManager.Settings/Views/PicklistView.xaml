<UserControl x:Class="IfcManager.Settings.Views.PicklistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IfcManager.Settings.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style TargetType="ComboBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={RelativeSource Self},
                                        Path=(Validation.Errors)[0].ErrorContent}" />

                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>

                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Top bar -->
            <RowDefinition Height="*"/>
            <!-- Scroll area -->

            <RowDefinition Height="50"/>
            <!-- Scroll area -->
        </Grid.RowDefinitions>

        <!-- ✅ TOP BAR -->
        <Grid Margin="5" Grid.Row="0">
            <Button Content="Add Picklist"
            Command="{Binding AddPicklistCommand}"
            Padding="5"
            Margin="5"
            HorizontalAlignment="Stretch"/>
        </Grid>

        <!-- ✅ SCROLL AREA -->
        <ScrollViewer Grid.Row="1"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">

            <ItemsControl ItemsSource="{Binding Picklists}">

                <!-- ✅ Horizontal columns -->
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>

                        <Border BorderBrush="Gray"
                                BorderThickness="1"
                                Margin="5">

                            <StackPanel Width="180">

                                <!-- HEADER -->
                                <DockPanel Background="#EEE">

                                    <ComboBox ItemsSource="{Binding DataContext.AvailablePicklistGroupNames, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}"
                                              SelectedItem="{Binding Name, 
                                                             UpdateSourceTrigger=PropertyChanged,
                                                             ValidatesOnNotifyDataErrors=True,
                                                             NotifyOnValidationError=True }"
                                              FontWeight="Bold"
                                              Width="120"
                                              Margin="5"/>

                                    <Button Content="+"
                                            Command="{Binding DataContext.AddItemCommand,
                                                      RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Margin="5"/>

                                    <Button Content="X"
                                            Margin="5"
                                            DockPanel.Dock="Right"
                                            Command="{Binding DataContext.RemovePicklistCommand,
                                                      RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}" />
                                </DockPanel>

                                <!-- ✅ INNER SCROLL (per column rows) -->
                                <ScrollViewer VerticalScrollBarVisibility="Auto"
                                              MaxHeight="300">
                                    <!-- control height -->

                                    <ItemsControl ItemsSource="{Binding Items}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>

                                                <DockPanel Margin="3">

                                                    <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                                             Width="100"/>

                                                    <Button Content="X"
                                                            Command="{Binding DataContext.RemoveItemCommand,
                                                                      RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding}"
                                                            Margin="3,0"/>

                                                </DockPanel>

                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                </ScrollViewer>

                            </StackPanel>
                        </Border>

                    </DataTemplate>
                </ItemsControl.ItemTemplate>

            </ItemsControl>

        </ScrollViewer>


        <Grid Grid.Row="2" Margin="5">
            <Button Content="Save"
            Command="{Binding SaveCommand}"
            Padding="5"
            Margin="5"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"/>
        </Grid>

    </Grid>

</UserControl>
