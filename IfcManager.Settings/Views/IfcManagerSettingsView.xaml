<Window x:Class="IfcManager.Settings.Views.IfcManagerSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IfcManager.Settings.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <TabControl>
        <TabItem Header="Paths">
            <local:PathsView DataContext="{Binding PathsViewModel}"/>
        </TabItem>

        <TabItem Header="Properties">
            <local:PropertiesView DataContext="{Binding PropertiesViewModel}"/>
        </TabItem>
    </TabControl>
</Window>
